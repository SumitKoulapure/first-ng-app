import { LowercaseTrunPipe } from './lowercase-trun-pipe';

describe('LowercaseTrunPipe', () => {
  it('create an instance', () => {
    const pipe = new LowercaseTrunPipe();
    expect(pipe).toBeTruthy();
  });
});
